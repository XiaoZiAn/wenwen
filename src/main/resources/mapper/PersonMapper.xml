<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="src.main.java.persons.mapper.PersonMapper">
    <insert id="insert" parameterType="src.main.java.persons.dao.PersonDao">
        insert into person (
            person_id,
            personname,
            person_age,
            person_sex,
            person_birthday,
            requestDate)
        values (
            #{personId, jdbcType=VARCHAR},
			#{personName, jdbcType=VARCHAR},
			#{personAge, jdbcType=INT},
			#{personSex, jdbcType=VARCHAR},
			#{personBirthday, jdbcType=VARCHAR},
			sysdate)
    </insert>
</mapper>