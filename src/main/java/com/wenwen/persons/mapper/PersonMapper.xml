<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenwen.persons.mapper.PersonMapper">
    <insert id="insert" parameterType="com.wenwen.persons.model.Person">
        insert into person (
            id,
            name,
            sex,
            birthday,
            email,
            password)
        values (
            #{id, jdbcType=VARCHAR},
			#{name, jdbcType=VARCHAR},
			#{sex, jdbcType=VARCHAR},
			#{birthday, jdbcType=VARCHAR},
			#{email, jdbcType=VARCHAR},
			#{password, jdbcType=VARCHAR})
    </insert>
    <select id="selectByPersonName" resultType="com.wenwen.persons.model.Person">
        select <include refid="Person_List" />
        from person p
        where p.name = #{name, jdbcType=VARCHAR}
    </select>
    <select id="selectAll" resultType="com.wenwen.persons.model.Person">
        select <include refid="Person_List"/>
        from person p
    </select>

    <select id="getByNameOrEmail" resultType="com.wenwen.persons.model.Person">
        select <include refid="Person_List"/> as password from person p where p.name = #{name,jdbcType=VARCHAR} or p.email = #{email,jdbcType=VARCHAR}
    </select>

    <sql id="Person_List">
        p.id,
        p.name,
        p.sex,
        p.birthday,
        p.createtime,
        p.password
    </sql>
</mapper>